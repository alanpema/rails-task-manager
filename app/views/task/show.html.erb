<h2><%=link_to "Edit Task", edit_path  %></h2>

<h1><%= @task.title %></h1>

<h3 ><%= @task.details %></h3>

<h2><%=link_to "All tasks", tasks_path(@tasks) %></h2>

<%= link_to "Delete",
  task_path(@task),
  data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}
%>
